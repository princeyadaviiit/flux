{
  "name": "flux-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Framework-agnostic library for building Agentic AI-Native web applications",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "vitest run",
    "playground": "vite playground",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "solid-js": "^1.9.15",
    "svelte": "^5.57.0",
    "turbo": "^2.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "vue": "^3.5.42"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
